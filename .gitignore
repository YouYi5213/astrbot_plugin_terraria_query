# 运行时生成的查询卡片，不入库
data/terraria_query/cards/

# 本地发版备忘，不上传
推送须知.md

# 发布 zip 资产包（随 GitHub Release 提供，不入库）
*.zip

# 内置中文字体（体积过大，不入库；Docker/Linux 请自行安装中文字体）
assets/fonts/

# 数据审计运行产物（scripts/audit_*.py 的输出）
scripts/audit_*_result.*

__pycache__/
*.py[cod]
*.log
.env
.pytest_cache/
